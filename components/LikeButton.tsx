export const LikeButton = () => {
  const number = 4;

  //   const { rows, fields } =
  //     await sql`SELECT * FROM projectid WHERE likes > 2 LIMIT 5;`;

  //   console.log("rows");

  return <p>{number}</p>;
};
